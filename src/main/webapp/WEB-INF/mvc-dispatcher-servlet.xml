<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd">

    <context:component-scan base-package="tran.example.basicwebapp.controllers.v1"/>

    <!-- specify a place to search for static resources. -->
    <!-- build/ is where the JavaScript application is built by Grunt. -->
    <mvc:resources mapping="/app/**" location="/app/build/"/>
    
    <mvc:annotation-driven/>

    <bean id="jspViewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- AOP point cut to apply the access rule of 'ROLE_DUMMY' to the execution of all the RESTful endpoints. -->
    <!-- This means you have to have the role of 'ROLE_DUMMY' to access the endpoints inside of the controllers.v1 directory. -->
    <!-- This is essentially a white-list but it can be overridden by @preAuthorize annotations in that if the user
    has a specified role then the below whitelist is essentially not executed. -->
    <security:global-method-security pre-post-annotations="enabled">
        <security:protect-pointcut expression="execution(* tran.example.basicwebapp.controllers.v1.*.*(..))"
                                   access="ROLE_DUMMY"/>
    </security:global-method-security>
    
</beans>
